# Seed handover — how to proceed from local disk

This folder is the pre-first-commit seed described in `cards/repo-seed-v0.2.md`. It was assembled on 3 Sept 2026 from the working outputs of three execution passes.

**Correction (3 Sept 2026, after import).** The handed-over folder already contained the portability rewrite that step 2 below describes as still to be done: no `.py` file contains `/home/claude/sid` or `/mnt/user-data/outputs`, every run script resolves `analysis/lib` and `figures/` from `__file__`, and there is no `SID_HOME` symbol anywhere. The first commit is therefore byte-identical to the folder as received, but its lineage is not a pure move from the original working outputs — the rewrite predates the repository. It is recorded under a name that says so. No artificial earlier commit was reconstructed.

## What is here
- `cards/v1.0-frozen.md` — the preregistered baseline, reproduced verbatim from the frozen text of 2 Sept 2026.
- `cards/v1.1-frozen.md`, `cards/v2.0-seed.md`, `cards/repo-seed-v0.2.md`, `cards/history/` (repo seed v0.1 and the one-page scope path).
- `notes/` 01, 02, 03, 03a — unchanged from the working folder.
- `labbook/` three retroactive entries written to the contract.
- `ledgers/` status, milestones, roadmap — populated from the notes; every claim carries its status.
- `analysis/` library, eight run scripts, stored outputs, `seeds.md`, output schema.
- `tests/` analytic tests with independent expectations (run in seconds). *Correction: there are no long Monte-Carlo checks and no `slow` marker — no `pytest.ini`, `setup.cfg` or `pyproject.toml` exists and no test carries a marker. Writing the Monte-Carlo tier is work-plan step 6.*
- `figures/` eight PNGs with a source-and-status table in `figures/README.md`. *Correction: that table labels one figure superseded (`sid_regime_map_tau_optimised.png`), not two.*
- `references/` bibliography and verification ledger.
- `docs/` site source stub; the companion introduction is included as NOT-CANONICAL.
- `tools/build_site.py` — generator with postconditions and freshness check.
- Licences from SPDX text (Apache-2.0, CC-BY-4.0).

## What is deliberately missing (owner supplies)
- The dated task-card drafts before v1.0 (six documents, 2 Sept 2026) — you hold the originals; drop them into `cards/history/` with their dates. `cards/history/scope-path.md` lists them.
- The fringe-sketch SVG referenced by the companion draft.
- ORCID, affiliation and contact fields in `CITATION.cff` / `codemeta.json` (marked TODO).
- Decision on code licence if `iontrap-dynamics` is not Apache-2.0.

## First actions — what was actually done (3 Sept 2026)

1. `git init`; the whole folder imported as one commit, tagged `archive-2026-09-03`. The commit is byte-identical to the folder as received; see the correction above for why it is not named a pure move.
2. **Not needed.** The path rewrite had already been applied before handover. What it left behind is real and separate: `import os` sits above the module docstrings in `analysis/lib/sid_lib.py` and `analysis/runs/sid_core.py`, demoting them to dead expressions, and `sid_run8.py` gained an unused `OUTD` while its write stayed on a bare relative path.
3. `pytest tests` passes. Three run scripts were taken end to end from a clean clone, in a scratch directory so no archived file was touched: `sid_run7.py` (identifiability and servo, reproduces to 6e-8 relative), `sid_run3.py` (crossover table and τ-optimised map, reproduces), `sid_run5.py` (writes no output file — confirming the `[unreproduced-from-file]` marker on C10).
4. The policy chain did **not** reproduce. See `notes/2026-09-03-note-04-reproduction-defect.md`: the documented command consumed its own committed output as cache, and two archived calibration rows cannot be generated by any code here. Repaired; `tests/test_reproduction_route.py` is the gate.
5. Claim C01 was found false at the project's reference contrast. See `notes/2026-09-03-note-01a-errata.md`.
6. Steps 2–9 of `cards/repo-seed-v0.2.md` remain open. The tag must not be pushed until the certification blockers are cleared.
