# 2026-09-03 — Pass 3: does the recorded evidence actually reproduce?

**Question:** The working folder became a repository. Before anything is published, does the documented reproduction path regenerate the numbers the notes report — and does every claim rest on code that exists?
**Finding:** No, in three distinct ways. The documented command consumed its own committed output as checkpoint state, so a clean clone recomputed nothing and reprinted the archive in zero seconds against a true cost of 7.7 minutes. Two of the seven calibration rows in note 03 §2 cannot be generated by any code here: five regenerate bit-identically, those two do not, and they fail even when the simulator is driven at their own archived thresholds — one is rounded to the log's print precision in all three fields, the other carries a threshold off the bisection lattice. Separately, C01's accuracy bound is false at the project's own reference contrast: "within 6 % for q ≤ 0.06" gives 7.3 % at C̄ = 0.4 and 10.1 % at 0.3, holding only for C̄ ≥ 0.5 — the contrasts the original check happened to sample.
**Why it matters:** The provenance failure is the central result of this pass, and it is not repaired by the numbers being plausible: two rows are withdrawn, four delay entries measured at those thresholds fall with them, and every ratio reported at τ_c/c = 20 divides by a withdrawn quantity. All four affected rows sit inside a 3σ band on the mean, so a means-only comparison would have passed them; only an exact-threshold test catches them. C01 is withdrawn and replaced by a contrast-dependent statement.
**Status:** result (C20, C21); withdrawn (C01, C17, C18); open (C09, C11, C12 demoted from pilot).
**Next test:** rerun the whole calibration-and-delay chain from a clean checkout under the pinned environment, which is what would let C09/C11/C12 stand again.

Technical record: notes/2026-09-03-note-01a-errata.md and notes/2026-09-03-note-04-reproduction-defect.md; sid_run6s.py rewritten to compute afresh by default with an explicit `--resume`, sid_run8.py to import shared machinery rather than exec a text slice of another script, sid_core.py extended with section A2; outputs res1_core.json (key A2); gate tests/test_reproduction_route.py, which runs with the committed archive present because that is the condition under which the defect was invisible. Environment: python 3.11.11 / numpy 2.2.4, and python 3.9.7 / numpy 1.23.5 for the cross-check; the pinned stack was not yet available. Seeds unchanged: calibration 100–104 and 199, delays 200 + int(τ_c/c).
