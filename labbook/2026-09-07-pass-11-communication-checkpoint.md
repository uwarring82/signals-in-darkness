# 2026-09-07 — Pass 11: a communication checkpoint, and a factor of four

**Question:** Can the project be explained accurately to someone with no background, and does writing it down survive contact with the record?
**Finding:** Mostly yes, and the exercise caught a real error. Notebook 01 shows two binary records with identical head counts and different ordering, and its central figure compares the sample lag correlations against the project's exact form. On first execution the sample values sat about five standard errors below the curve. The cause was mine: `rk_exact` **already returns a correlation** — the project normalises by the mid-fringe binary variance of ¼, which is precisely the normalisation named in C02's own statement — and I divided by ¼ a second time, putting the theory curve 4× too high. Empirically the lag-1 correlation converges to 0.0294 against `rk_exact` = 0.029913. Caught only by reading the notebook's printed output against its own claim.
**Why it matters:** The notebook would have been internally consistent and externally wrong — figure, prose and claim label all agreeing on a curve that was four times the truth, in a notebook whose stated purpose is to show that the sample correlations follow the project's exact form. Two further honesty repairs followed. The 4000-toss record cannot resolve a correlation of 0.03 with a standard error of 0.016, so the notebook now shows both a short and a 200,000-toss record and says why — which turns a presentational problem into the pedagogical point that evidence carried by ordering accumulates slowly, and is the reason the project measures detection delay rather than asking whether a difference exists. And the mid-fringe marginal is exactly one half for any contrast, so a count-based test at that readout has not merely low power but **none**; that is the sharpest available form of "balance versus memory" and it is now the notebook's spine.
**Status:** No claim changed. Notebook 01 satisfies all eight contract items; `docs/where-we-are.md` and a rewritten `docs/index.md` are checked against the ledger by a new `tests/test_public_prose.py`.
**Next test:** G — the immutable operating-point configuration.

Technical record: no note; this pass produced explanatory surface, not evidence. The public pages inherit the notebook discipline: claim identifiers and statuses verified against `ledgers/status.yaml`, a section that presents claims as established asserted to contain only `result` claims, every withdrawn claim required to appear in the corrections section, stated counts matched against the ledger, and internal links resolved. One of those tests had to be rewritten immediately: matching the withdrawn count by substring failed because "none of them" contains "one of them" — a check that cannot distinguish those is worse than no check. The NOT CANONICAL companion introduction stays unlinked and a test enforces that; it describes settled results as proposed, predates the attribution boundary, and is left for rewriting after B and the v2.0 freeze. Notebook 02 is deferred until B so that it teaches the final operating-point comparison rather than an interim one, which also lets notebook 03 rest on the settled C26 instead of the withdrawn C13.
