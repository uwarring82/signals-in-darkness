# 2026-09-07 — Pass 7: locking the manifest found the defects

**Question:** What conventions must roadmap items B and E share, and are they already fixed somewhere in the repository?
**Finding:** They are fixed, in three disjoint stacks rather than the two anyone had named — shot (dimensionless τ_c/c, no dead time), physical (c = τ + t_dead), and identifiability (τ_c/T₂, and no dead time at all: `sid_run7.py:20` assigns `td = 1.0` and never uses it). The manifest is `analysis/manifest_BE.json`, and `tests/test_manifest_BE.py` asserts every value it calls `source: code` against the source and every hazard it names against the archive, so it cannot quietly go stale. The exercise was meant to be bookkeeping and was not: it turned up that `u = τ/τ_c`, not `c/τ_c` — so the AR(1) reduction of roadmap F is exact as the *interrogation window* shrinks against the correlation time, not as the cycle does — and that the committed helper `I_mid_lo` omits the factor e^{−2s²} that C02's own statement says the leading-order rate carries. The helper is 65 % high at s = 0.5, it is imported into `test_analytic.py` and never called, and its value is stored as a column in two archived outputs.
**Why it matters:** One published number is mis-attributed. C25 reports that "the card leading order would give 910" at τ_c/c = 20; recomputation gives 907.1 from the helper and 1495.6 from the card's own formula, so the figure came from the helper that drops the card's factor. C25's conclusion survives — 1496 against a measured 2010 ± 170 is still not a usable delay predictor — but the failure is 26 % low rather than 55 %, and the attribution is wrong. Recorded as an erratum; nothing was silently corrected, because fixing `I_mid_lo` rewrites archived columns and is a regeneration event that deserves its own commit and its own errata.
**Status:** unchanged (C25 stays `result`, with an erratum; C02 stays `result`, its statement being the correct one and the code the defective one). No claim demoted.
**Next test:** E, once D1 is answered. B is not ready: it needs D1 and D2, the mutable-global hazard resolved, and a fresh τ_c/c = 20 calibration that does not touch the withdrawn C17/C18 rows.

Technical record: notes/2026-09-07-note-13-BE-manifest.md. Three decisions are escalated rather than assumed. D1: nothing states whether the proposed C̄ = 0.8 is the effective contrast C̄(τ) or the zero-τ contrast C̄₀, which differ by e^{−τ/T₂}. D2: C̄ = 0.8 exceeds the parity ceiling C̄_par ≤ ½ (card v1.1:117), and card v1.1:132 says contrasts above ½ require the full four-outcome likelihood — so B as specified puts a freeze-gating run in the regime card v2.0 item 3 relegated to an appendix, and makes it depend on roadmap D, which is non-gating and post-freeze. D3: item E asks for two calibration uncertainties and `res7A` already carries three. Separately, C̄ = 0.8 is not a computed value anywhere: it is a rounded reading of note 03 §5, whose nearest computed entries are 0.8126 and 0.8073 under a claim that is `pilot`. δ(0) at (0.8, 0.3) is 0.061 / 0.476 / 2.054 at τ_c/c = 1 / 5 / 20 against C23's validity level 0.3, so two of the three proposed grid points sit outside leading-order mid-fringe validity; the exact comparator carrying C08 is unaffected.
