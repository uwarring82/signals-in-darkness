# 2026-09-07 — Pass 14: freezing the object does not freeze what was derived from it

**Question:** Was roadmap G actually complete when pass 13 said so?
**Finding:** No. Four defects survived, and the first is the instructive one. `sid_run8.py` had its `from sid_policies import C, s` removed — and then rebound the same names one line below as `C, s = OP.C_eff, OP.s`. `run_batch_age` drew its post-change process from those module names while its filter and null came from `bank.op`, so a bank built at C_eff = 0.5, s = 0.3 would have been simulated against the pilot's C_eff = 0.4, s = 0.5 process, silently. **Making the operating point immutable does not make a binding derived from it immutable**, and the whole point of the refactor was to remove the possibility of that mixture. Its checkpoint also still had no identity at all — no operating point, no digest, no compatibility check — so it could be resumed across operating points, which is the exact defect G exists to eliminate, left intact in the second driver.
**Why it matters:** The acceptance tests I wrote forbade *importing* `C` and `s` and not *rebinding* them, which is why they passed over the defect they were written to catch. That is the third time in this repository a check has been shaped to the letter of a fix rather than its intent — after the vacuous `assert … or True` in pass 13 and the absolute-versus-relative spread in pass 12. The tests now walk the module AST of both drivers and reject any module-level assignment to those names. And the central preservation test skipped when the git-ignored reproduction file was absent, which is always on a fresh clone, so G's numerical claim lived only in a terminal transcript bound to no commit; it is now a committed artifact asserted unconditionally.
**Status:** No claim changed; the refactor remains numerically inert, now demonstrated from a clean tree and recorded. Roadmap G complete after the repair pass.
**Next test:** B, at C_eff = 0.5, s = 0.3 — now genuinely unblocked, since both simulators read their operating point from the bank and both drivers refuse a mismatched checkpoint.

Technical record: notes/2026-09-07-note-17-roadmap-G.md, whose repair section is appended rather than replacing the premature completion claim. Evidence at analysis/G_preservation_evidence.json, from a clean-tree calibration at b927e6e, run d2a1960161d7, python 3.9.7 / numpy 1.23.5: the five valid pilot rows exact, the two C17-withdrawn rows failing as required. The first attempt to produce that evidence recorded `b927e6e+dirty`, because the tests were being edited while the calibration ran — the same provenance failure G exists to remove, reintroduced during the work to remove it; the run was discarded and repeated from a stashed-clean tree. One rule was deliberately narrowed: the recorder refuses outright if the calibration ran dirty, which is load-bearing, but only *records* the tree state at recording time, since the tool and its test cannot be committed before the artifact they gate exists. A test asserts every path the artifact names as uncommitted actually exists, because the first version of that parsing used `stdout.strip()` and cut a character off the first filename.
